import re
import pandas as pd
import numpy as np
import twitter

#Fill in your twitter developer information to run the api
api = twitter.Api(consumer_key=[consumer key],
                  consumer_secret=[consumer secret],
                  access_token_key=[access token],
                  access_token_secret=[access token secret])
#search for given twitter searches, returns a json list for tweet RNN
GetSearch(term=none, return_json=True)

#opens a stream for implementation after tweet RNN has been trained
GetStreamFilter('import twitter lists to here')


